import styled from "styled-components";

export default styled.section`
  margin-bottom: 15rem;
  .title-sub-wrap {
    //height: 100vh;
    display: flex;
    /* align-items: center; */
    justify-content: center;
  }
  .box-title,
  .box-content {
    font-weight: bold;
  }

  .box-img-wrap img {
    width: 70px;
  }
`;
